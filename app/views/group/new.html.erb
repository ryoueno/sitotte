<%= form_for @group, :url => {:action => :create} do |f| %>

  <div class="field">
    <%= f.text_field :name %>
  </div>
  <%= f.hidden_field :admin_id, {'value' => current_user.id} %>

  <div class="actions">
    <%= f.submit "登録する" %>
  </div>
<% end %>
